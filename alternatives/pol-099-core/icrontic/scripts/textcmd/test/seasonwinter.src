use uo;

include "include/packets";

program textcmd_desolateseason( who )
  SetSeasonForAll( SEASON_WINTER );
  SendSysMessage(who, "Season Winter Set for all players");
  SetGlobalProperty("gseason", "SEASON_WINTER");
endprogram
