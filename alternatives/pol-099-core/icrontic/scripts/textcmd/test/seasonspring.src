use uo;

include "include/packets";

program textcmd_desolateseason( who )
  SetSeasonForAll( SEASON_SPRING );
  SendSysMessage(who, "Season Spring Set for all players");
  SetGlobalProperty("gseason", "SEASON_SPRING");
endprogram
