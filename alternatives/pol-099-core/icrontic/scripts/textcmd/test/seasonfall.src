use uo;

include "include/packets";

program textcmd_desolateseason( who )
  SetSeasonForAll( SEASON_FALL );
  SendSysMessage(who, "Season Fall Set for all players");
  SetGlobalProperty("gseason", "SEASON_FALL");
endprogram
