use os;
use uo;

program tigerstone( who, tigerstone )

    var place;

	place := CreateItemInBackpack(who, 0xb000);
	place := CreateItemInBackpack(who, 0xb001);
	place := CreateItemInBackpack(who, 0x3e0);
      SendSysMessage( who, "You find a forge, anvil and a Smith's dream stone in your pack");
    DestroyItem (tigerstone);
      return;

endprogram
