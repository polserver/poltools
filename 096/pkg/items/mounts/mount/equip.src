/* $Id$
 *
 */
use uo;

program EquipScript(mobile, item, start_up)

	// Next 3 vars, used to fux unused var report by ecompile
	mobile := mobile;
	item := item;
	start_up := start_up;

	return 1;

endprogram