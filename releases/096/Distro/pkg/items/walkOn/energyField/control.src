/*
 * $Id: control.src 128 2006-05-27 17:36:42Z austinheilman $
 *
 */

use uo;
use os;

program control(field)
	while ( field )
		Sleep(1);
		if ( field.IsExpired() )
			DestroyItem(field);
			return 1;
		endif
	endwhile
endprogram
