use os;

var script := Start_Script( ":doom:doomControl/doomControl" );
if( script.errortext )
	SysLog( "Error starting doomControl ->"+script.errortext );
else
	Print( "Starting doomControl... OK." );
endif
