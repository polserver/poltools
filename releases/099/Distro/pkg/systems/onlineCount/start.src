/*
        Created by *Edwards

        2010-05-28
*/
use os;

var script := Start_Script( "cheatCount" );
if( script.errortext )
	SysLog( "Error starting cheat count ->"+script.errortext );
else
	Print( "Starting cheat count... OK." );
endif
