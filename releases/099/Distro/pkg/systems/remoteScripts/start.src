/*
        Created by *Edwards

        2011-07-02
*/
use os;

var script := Start_Script( "remoteScriptsControl" );
if( script.errortext )
	SysLog( "Error starting remote scripts system ->"+script.errortext );
else
	Print( "Starting remote scripts system... OK." );
endif
