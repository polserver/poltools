use uo;

program EnterRegionZone( mobile, region_name )

        region_name := region_name; //Avoid compiler warnings
	
	SendSysMessage( mobile, "You have left the protection of the town guards.", 3, 50 );
	
        return 1;
endprogram
