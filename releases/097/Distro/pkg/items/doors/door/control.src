/* $Id: control.src 87 2006-05-23 19:04:43Z sroyalty $
 * 
 * Purpose:
 * Closes doors on startup.
 *
 */

use uo;

program controlScript(door)
	if ( !door.locked )
		if ( door.IsOpen() )
			door.Close();
		endif
	endif
	
	return 1;
endprogram
