/*
 * $Id: control.src 128 2006-05-27 17:36:42Z austinheilman $
 *
 */

use uo;
use os;

program RemoveWeb(web)
	while ( web )
		Sleep(1);
		if ( web.IsExpired() )
			DestroyItem(web);
			break;
		endif
	endwhile
endprogram