/* $Id$
 *
 */

use uo;
use os;

program onRemove(who, backpack, item, movetype)

	movetype := movetype;
	backpack := backpack;
	who := who;
	EraseObjProperty(item, "#DestroyAt");

	return 1;
endprogram
