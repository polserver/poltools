/* $Id: canDestroy.src 811 2006-09-13 20:20:37Z austinheilman $
 *
 */

use uo;


program canDestroy(container)
	/*
	foreach item in ( EnumerateItemsInContainer(container) )
		if ( item.container == container )
			MoveObjectToLocation(item, container.x, container.y, container.z, container.realm, MOVEOBJECT_FORCELOCATION);
		endif
	endforeach
	*/

	return 1;
endprogram
